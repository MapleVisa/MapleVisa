// No demo accounts are seeded. The first admin is created by signing up with
// the auto-admin email (h.izadian1397@gmail.com), which is granted ADMIN and
// skips email verification. Everyone else verifies their email before signing in.
console.log(
  "Nothing to seed. Create the first admin by signing up with the auto-admin email " +
    "(h.izadian1397@gmail.com); all other users verify their email before logging in."
);
